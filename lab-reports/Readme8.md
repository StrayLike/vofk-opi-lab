# ### 📝 Звіт з контейнеризації проєкту (Лабораторна робота №8)

---

### ### Огляд проєкту

* **Застосунок:** *Stardew Valley Fan Hub*
* **Технології:** Flask + SQLite
* **Мета контейнеризації:**

  * Забезпечення стабільного та швидкого розгортання
  * Створення архітектури, наближеної до production
  * Використання Nginx як reverse proxy

---

### ### Архітектура контейнерного рішення

* **Docker образ**

  * Базовий образ: `python:3.11-alpine` (фінальний)
  * Розмір фінального образу: *(вкажіть свій результат, зазвичай <150MB)*
  * Багатоетапна збірка: **Так**

* **Docker Compose**

  * Кількість сервісів:

    * `web` — Flask
    * `nginx` — reverse proxy
  * Використані volumes:

    * `sqlite_data` → зберігає `database.db`

---

### ### Прийняті рішення та обґрунтування

* **Вибір базового образу**

  * Обрано `python:3.11-alpine`
  * Причини:

    * Легкий
    * Мінімальний розмір
    * Швидке розгортання

* **Організація збереження даних**

  * **Проблема:** SQLite зберігає дані у файлі, який видаляється після перезапуску контейнера
  * **Рішення:**

    * Створено named volume `sqlite_data`
    * Підмонтовано до `/app/data`
    * Дані зберігаються на хості
  * **Адаптація коду:**

    * У `db.py` та `app.py` використано змінну середовища `DATABASE_PATH`

* **Оптимізації**

  * Використано Multi-stage build
  * Всі компілятори встановлені в builder-етапі
  * Nginx працює як reverse proxy
  * Використано `.dockerignore` для пришвидшення збірки

---

### ### Інструкції з розгортання

1. Завантажити файли:

   * `Dockerfile`
   * `docker-compose.yml`
   * `.env`
   * `nginx.conf`
2. Виконати очищення та повну збірку:

```bash
docker-compose down -v
docker-compose up -d --build
```

3. Перевірити Healthcheck:

```bash
docker-compose ps
```

4. Відкрити застосунок:
   👉 **[http://localhost](http://localhost)**

---

### ### Можливі покращення

* **HTTPS:** Додати автоматичний випуск SSL (Let's Encrypt / Certbot)
* **Backup:** Реалізувати резервне копіювання `database.db`

---

### ### Висновки

* Успішно завершено контейнеризацію проєкту
* Створена надійна, масштабована інфраструктура
* Проєкт відповідає вимогам **високого рівня (10–12 балів)**

---

### ### Досягнення у контейнеризації

* **Багатоконтейнерність (Nginx):**

  * Побудовано production-ready архітектуру
  * Розподілено ролі між веб-сервером та застосунком

* **Оптимізація образу:**

  * Завдяки Multi-stage та Alpine образ став легким і швидким

* **Персистентність даних:**

  * Volume `sqlite_data` гарантує збереження всіх даних (товари, замовлення, відгуки)

* **Надійність (Healthcheck):**

  * Docker перевіряє `/api/v1/status` та керує перезапусками

---

### ### Підсумок функціоналу

* **Backend:**

  * CRUD для товарів, відгуків
  * Логіка замовлень
* **Frontend:**

  * `/manage` — робота через Fetch API
  * Асинхронність, валідації Email, обробка помилок
* **Deployment:**

  * Розгортання в один клік через Compose
  * Повна консистентність середовища

---

### 🌟 Фінальний висновок

Проєкт повністю готовий до хмарного деплойменту та демонструє відмінне володіння Docker, архітектурою контейнерів та вебтехнологіями.
