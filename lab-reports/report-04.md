# 📝 Звіт з лабораторної роботи №4

## Реалізація бази даних для вебпроєкту (Stardew Valley Fan Hub)

### Інформація про команду

  - Назва команди: **Stardew Hub**

  - Учасники:

      - **Мартинюк Максим** (Team Lead, Архітектура БД та Авторизація)
      - **Алєєв Іван** (Developer, Система Відгуків)
      - **Гаврилюк Любомир** (Developer, Магазин та Фільтрація товарів)
      - **Віннік Артем** (Developer, Кошик, Оформлення Замовлень та Історія)

-----

## Завдання

### Обрана предметна область

Предметна область: **Фан-сайт та Інтернет-магазин ігрових товарів Stardew Valley**.

### Реалізовані вимоги

Команда виконала всі вимоги аж до **Високого Рівня (Рівень 3)**.

  - [x] **Рівень 1:** Створено базу даних SQLite з таблицею для відгуків, реалізовано базові CRUD операції, додано функціональність магазину з таблицями для товарів та замовлень, реалізовано зв'язки.
  - [x] **Рівень 2:** Створено додаткову таблицю **`users`** для аутентифікації.
  - [x] **Рівень 3:** Розширено функціональність: **Система користувачів з ролями**, **Історія замовлень** для користувачів та **Фільтрація товарів** за категоріями.

-----

## Хід виконання роботи

### Підготовка середовища розробки

  - Версія Python: **Python 3.11**
  - Встановлені бібліотеки: **Flask, SQLite3**, **Werkzeug**.
  - Інші використані інструменти: **Git**, **GitHub**, **Jinja2**, **Tailwind CSS**.

### Структура проєкту

```
project/
├── app.py             
├── db.py              
├── schema.sql         
├── templates/
│   ├── base.html      
│   ├── shop.html      
│   ├── cart.html      
│   └── ...
└── database.db        
```

### Проектування бази даних

#### Схема бази даних (5 таблиць)

| Таблиця | Призначення | Ключові поля та зв'язки |
| :--- | :--- | :--- |
| **`users`** | Аутентифікація, ролі | `id` (PK), `username`, `password` (хеш), `role` |
| **`products`** | Каталог магазину | `id` (PK), `name`, `price`, `category` |
| **`feedback`** | Відгуки користувачів | `id` (PK), `username`, `text`, `rating` |
| **`orders`** | Інформація про замовлення | `id` (PK), `user_id` (FK $\rightarrow$ `users`), `total_price`, `status` |
| **`order_items`** | Деталізація замовлення (Many-to-Many) | `id` (PK), `order_id` (FK $\rightarrow$ `orders`), `product_id` (FK $\rightarrow$ `products`), `quantity` |

-----

## Опис реалізованої функціональності

### Система відгуків

Реалізовано функціонал збору (`Create`) та відображення (`Read`) відгуків на сторінці `/feedback`. Функція **видалення** відгуків доступна лише для **адміністраторів**.

### Магазин

Реалізовано повний цикл електронної комерції:

1.  **Каталог:** Сторінка `/shop` виводить товари та має функціонал **фільтрації** за `category` (Рівень 3).
2.  **Кошик:** Товари тимчасово зберігаються у сесії Flask (`session['cart']`).
3.  **Оформлення замовлення:** Маршрут `/checkout` записує дані у таблиці `orders` та `order_items` (зв'язок).

### Адміністративна панель

Доступ до адмін-функціоналу контролюється перевіркою ролі (`admin`). Реалізовано перегляд усіх замовлень та можливість видалення відгуків.

### Додаткова функціональність (Рівень 3)

1.  **Система користувачів з ролями:** Реалізовано аутентифікацію (`/login`, `/register`) з використанням хешування паролів **Werkzeug**.
2.  **Історія замовлень:** Реалізовано сторінку `/profile`, де користувач бачить усі свої попередні замовлення (використання **JOIN**).

-----

## Ключові фрагменти коду

### CRUD операції: Створення (Create)

```python
from werkzeug.security import generate_password_hash
# ...
hashed_pw = generate_password_hash(password)
db.execute('INSERT INTO users (username, email, password) VALUES (?, ?, ?)',
           (username, email, hashed_pw))
db.commit()
```

### Робота зі зв'язками між таблицями (JOIN)

```python
# Запит для отримання історії замовлень користувача (Рівень 3)
def get_user_order_history(user_id):
    db = get_db()
    details = db.execute('''
        SELECT 
            o.status, o.created_at, oi.quantity, p.name AS product_name
        FROM orders o
        JOIN order_items oi ON o.id = oi.order_id
        JOIN products p ON oi.product_id = p.id
        WHERE o.user_id = ?
        ORDER BY o.created_at DESC
    ''', (user_id,)).fetchall()
    return details
```

-----

## Розподіл обов'язків у команді

  - **Мартинюк Максим:** Проектування схеми БД, реалізація **`db.py`**, налаштування **аутентифікації** та інтеграція всіх компонентів у **`app.py`**.
  - **Алєєв Іван:** Реалізація системи **відгуків** (`/feedback`), включаючи форму та логіку CRUD (створення/видалення).
  - **Гаврилюк Любомир:** Створення шаблону **магазину** (`shop.html`), заповнення тестовими даними та реалізація логіки **фільтрації товарів**.
  - **Віннік Артем:** Реалізація функціоналу **кошика** (`/cart`), **оформлення замовлення** (`/checkout`) та логіки для **історії замовлень** (`/profile`).

-----

## Тестування

### Сценарії тестування

1.  **Аутентифікація:** Успішна реєстрація нового користувача та вхід під роллю `user` та `admin`.
2.  **CRUD Відгуків:** Додавання нового відгуку; видалення відгуку (перевірка доступу `admin`).
3.  **Магазин та Фільтрація:** Перевірка відображення товарів; тестування фільтрації за категоріями (Рівень 3).
4.  **Кошик та Замовлення:** Додавання товарів у кошик; оформлення замовлення; перевірка записів у таблицях `orders` та `order_items`.
5.  **Історія Замовлень:** Перевірка сторінки `/profile` на коректне відображення замовлень (використання JOIN).

-----

## Висновки

**Оцінка:** **11 балів**

Обґрунтування:
Команда повністю виконала вимоги **Високого Рівня (Рівень 3)**. Ми реалізували складну реляційну схему БД (5 таблиць), ефективно використали **JOIN** для історії замовлень та впровадили три додаткові функції, що покращують UX: **систему ролей, фільтрацію товарів та історію замовлень**. Демонстрація всіх цих аспектів підтверджує глибоке розуміння принципів роботи динамічного вебзастосунку.

```
```
