Лабораторна робота 6: Інтеграція фронтенду з API
Студенти: Мартинюк Максим, Алєєв Іван, Гаврилюк Любомир, Віннік Артем Група: [Ваша група]

📋 Опис проєкту
У цій лабораторній роботі ми розробили SPA (Single Page Application) сторінку "Адмін-панель" для вебзастосунку Stardew Valley Fan Hub. Ця сторінка дозволяє адміністраторам керувати контентом сайту без перезавантаження сторінки, використовуючи асинхронні запити (Fetch API) до нашого Flask-бекенду. Реалізовано повний цикл керування товарами, модерацію відгуків та перегляд історії замовлень.

📁 Структура проєкту
project/
├── app.py                  # Головний файл Flask (Backend + маршрути сторінок)
├── api.py                  # RESTful API (Endpoints для Products, Feedback, Orders)
├── db.py                   # Робота з базою даних SQLite
├── templates/              # HTML-шаблони
│   ├── base.html           # Базовий шаблон (навігація)
│   ├── manage.html         # Адмін-панель (Фронтенд Лаби 6)
│   └── ...                 # Інші сторінки
├── lab-reports/            # Звіти
│   └── lab06-report.md     # Цей файл
└── requirements.txt        # Залежності (Flask, flask-cors)
🔌 API Endpoints
Наша адмін-панель взаємодіє з наступними ендпоінтами:

1. Товари (Products)
GET /api/v1/products
Отримує список всіх товарів для відображення в адмінці. Відповідь:

JSON

[
  {
    "id": 1,
    "name": "Зоряна крапля",
    "price": 5000.0,
    "category": "Артефакти",
    "image": "/static/images/stardrop.png"
  }
]
POST /api/v1/products
Створює новий товар (потребує прав адміністратора). Тіло запиту:

JSON

{
  "name": "Золотий Гарбуз",
  "price": 500,
  "category": "Насіння",
  "image": "url_to_image"
}
DELETE /api/v1/products/{id}
Видаляє товар за ID.

2. Відгуки (Feedback)
GET /api/v1/feedback
Отримує список всіх відгуків для модерації.

POST /api/v1/feedback
Створює новий відгук. Включає валідацію email на стороні клієнта та бекенду. Тіло запиту:

JSON

{
  "username": "Ivan",
  "email": "ivan@example.com",
  "text": "Чудовий сайт!",
  "rating": 5
}
DELETE /api/v1/feedback/{id}
Видаляє відгук (функція модератора).

3. Замовлення (Orders)
POST /api/v1/orders
Оформлює нове замовлення. Перевіряє наявність користувача в базі за username/email перед створенням. Тіло запиту:

JSON

{
  "username": "Max",
  "email": "max@example.com",
  "items": [{"product_id": 1, "quantity": 2}]
}
GET /api/v1/orders
Повертає історію всіх замовлень для адміністратора.

📸 Скріншоти
Головна сторінка адмін-панелі (Товари)
Створення та модерація відгуків
Повідомлення про успішне створення (Success)
Обробка помилок (Валідація)
🔗 Посилання
Посилання на GitHub Репозиторій

✅ Висновки
У ході виконання лабораторної роботи ми навчилися інтегрувати фронтенд (HTML/JS) з Python-бекендом (Flask) за допомогою технології Fetch API. Ми освоїли:

Налаштування CORS у Flask для дозволу асинхронних запитів.

Використання async/await для отримання та відправки даних (GET/POST/DELETE) без перезавантаження сторінки.

Динамічне оновлення DOM-дерева (списків товарів та відгуків) на основі JSON-відповідей.

Реалізацію валідації даних (зокрема Email) на стороні клієнта JavaScript та обробку помилок, що повертає сервер.